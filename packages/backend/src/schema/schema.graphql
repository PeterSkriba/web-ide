type Code {
  id: ID!
  user_id: ID!
  exercise_id: ID!
  body: String!
  tests: [Boolean]!
}


type User {
  id: ID!
  first_name: String!
  last_name: String!
  email: String!
  password: String!
  codes: [Code]
}

type Query {
  code: Code
}